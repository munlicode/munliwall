{
  "name": "@munlicode/munliwall-cli",
  "version": "1.1.4",
  "type": "module",
  "description": "A simple yet powerful tool designed to transform your workspace with ease. Wallpaper lets you effortlessly manage your desktop backgrounds — fetch new wallpapers by keyword, source, or category, schedule automatic updates, or quickly apply local images via the command line.",
  "author": "Nurzhan Muratkhan <nurzhanmuratkhan@kreoza.com>",
  "bin": {
    "munliwall": "dist/cli.js"
  },
  "pkg": {
    "scripts": "dist/cli.js",
    "assets": [],
    "outputPath": "dist"
  },
  "scripts": {
    "build": "npx esbuild src/cli.ts --bundle --minify --platform=node --format=cjs --outfile=dist/cli.js --external:wallpaper --external:electron",
    "dev": "tsc --watch",
    "dist": "pnpm run build && pkg package.json --targets node18-linux-x64,node18-macos-x64,node18-win-x64 --out-dir dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@types/yargs": "^17.0.32",
    "@munlicode/munliwall-core": "file:../core",
    "dotenv": "^16.4.5",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "esbuild": "^0.19.0"
  }
}
